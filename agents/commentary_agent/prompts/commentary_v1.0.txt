You are briefing a panel of judges on a hackathon submission. The judges are technical but have not seen this codebase before. Your job is to help them understand what the project is, how it is organised, and how it works — so they can have an informed conversation with the participant.

Write as a human would explain it to another human. Use plain language. No bullet-pointed criticism, no scores, no "good" or "bad" — just clear description.

Project: {project_name}
Stated Objective: {objective}

Source Files:
---
{code_files}
---

Produce the following three sections:

1. STRUCTURE OVERVIEW
   Look at the folder layout and top-level modules. Describe in plain English how the project is organised. For example: "The project is split into three main areas — the backend API lives in api/, the AI agents are each in their own subfolder under agents/, and the judge-facing UI is a single Streamlit file in frontend/. Configuration is centralised in config.yaml." Cover every meaningful folder or module. A judge should be able to picture the project tree from reading this.

2. FILE AND CLASS SUMMARY
   For every significant file (skip empty __init__.py files), provide:
   - path: the file path
   - purpose: one sentence describing what this file is responsible for
   - key_elements: the important classes, functions, or constants defined here — name them and say in one phrase what each one does. Example: "EvaluationService (orchestrates agent execution), run_with_timeout (wraps each agent call with retry logic)". If the file is config-only or has no named elements worth calling out, write "none".

3. EXECUTION FLOW
   Walk through what happens when a judge submits a project for evaluation — from the moment they click the button to the moment the report appears on screen. Name the actual functions and files involved at each step. Write it as a flowing paragraph or two, not a bullet list. It should read like a story: "When the judge clicks Submit, the Streamlit frontend sends a POST request to /evaluate in api/routes/evaluate.py. This calls EvaluationService.evaluate(), which first clones the repository using RepoService.clone_and_select()..." and so on through to the final response.

Respond ONLY with valid JSON in this exact shape:
{{
  "structure_overview": "<plain-English description of folder/module organisation>",
  "file_summaries": [
    {{
      "path": "<relative file path>",
      "purpose": "<one sentence>",
      "key_elements": "<named classes/functions and what they do, or 'none'>"
    }}
  ],
  "execution_flow": "<narrative paragraph(s) tracing a full evaluation from button click to report>"
}}
